//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/////////////////////////           Authored and Copyrighted by Wilfido Castaneda    /////////////////////////////////////////
//////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#include <iostream>
#include "LinkedListCPP.h"
#include <stdexcept>

int main()
{

	///////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	////////////////////////////////////                TESTING: PART A        ////////////////////////////////////////
	///////////////////////////////////////////////////////////////////////////////////////////////////////////////////
	LinkedList* newThing = new LinkedList();

	newThing->add(0, 50 ); //adding to empty list
	newThing->addFirst( 90 ); //adding to front of a list
	newThing->add( 1, 85 ); //adding to middle of list
	newThing->add( 3, 54050 ); //adding to end of list
	newThing->add( 5, 666 ); //adding to out of bounds

	printf( "The size of the current LinkedList is now %d.\n", newThing->sizeOf() );
	newThing->printList();

	char a;
	scanf_s( &a );
	return 0;
}